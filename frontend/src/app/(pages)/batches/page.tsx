import EntityGrid from "@/components/ui/EntityGrid";

const Batches = () => {
  return (
    <EntityGrid
      itemType="batch"
      className="pt-28 md:pt-40 pb-20"
    />
  );
};

export default Batches;
